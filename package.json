{
  "name": "@david.uhlir/shared-cache",
  "version": "0.0.3",
  "description": "Cache shared over all forks in cluster",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "prettier-format": "prettier --config .prettierrc.json 'lib/**/*.ts' --write",
    "release": "./scripts/release.sh",
    "test": "echo nothing"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/daviduhlir/shared-cache.git"
  },
  "keywords": [],
  "author": "David Uhlíř",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.3.0",
    "prettier": "^2.6.2",
    "rimraf": "^2.6.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@david.uhlir/ipc-method": "^1.0.22"
  }
}
