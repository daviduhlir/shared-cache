{
  "name": "shared-cache-example",
  "version": "1.0.0",
  "description": "",
  "main": "./build/index.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node --enable-source-maps ./build/index.js"
  },
  "author": "David Uhlíř",
  "license": "ISC",
  "dependencies": {
    "@david.uhlir/shared-cache": "file:.."
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "rimraf": "^2.6.2",
    "typescript": "^5.9.2"
  }
}
